let eded = prompt(`Eded daxil edin`);

let sol = Number(eded[0]) + Number(eded[1]) + Number(eded[2]);
let sag = Number(eded[3]) + Number(eded[4]) + Number(eded[5]);

if (sol === sag) {
  console.log(`Bilet sanslidir`);
} else {
  console.log(`Bextinizi bir de sinayin`);
}


